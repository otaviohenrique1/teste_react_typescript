import React, { Component } from 'react'

/* type ContadorClassProps = {}

type ContadorClassState = {} */

export default class TesteClasse extends Component/* <ContadorClassProps, ContadorClassState> */ {
  /* constructor(props: ContadorClassProps) {
    super(props);
    this.state = {};
  }

  componentDidUpdate(prevProps: Readonly<ContadorClassProps>, prevState: Readonly<ContadorClassState>, snapshot?: any): void {
    // 
  }

  componentDidCatch(error: Error, errorInfo: React.ErrorInfo): void {
    // 
  }

  componentDidMount(): void {
    // 
  }

  componentWillUnmount(): void {
    // 
  } */

  render() {
    return (
      <div>TesteClasse</div>
    )
  }
}
